source "https://rubygems.org"

# This is the key: it pulls in the *exact* set of gems
# that GitHub Pages uses for its default builds.
# This prevents "it worked on my machine" errors.
gem "github-pages", group: :jekyll_plugins
